// Importar controladores
// TODO: CREAR E IMPORTAR CONTROLADORES

// Crear Router de Express
const express = require("express");
const router = express.Router();

// TODO: DEFINIR LAS RUTAS
// Definir las rutas
router.get("/:id", ()=>{});
router.get('/', ()=>{});
router.get('/', ()=>{});
router.post('/', ()=>{});
router.post('/', ()=>{});
router.delete('/:id', ()=>{});

// Exportar el router
module.exports = router;
